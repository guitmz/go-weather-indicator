language: go

go:
  - tip

before_script:
  - sudo apt-get update -qq
  - sudo apt-get install -y gdk-pixbuf libgtk2.0-dev
  - go get github.com/guitmz/go-weatherbit
  - go get github.com/mattn/go-gtk

script:
   - go build -i -v .
